# devtribunal (remove these lines to version-control agents and skills)
.devtribunal_agents/
.claude/commands/dt/

# Track data/ and logs/ as directories, but exclude their contents.
data/*
!data/.gitkeep
# Durable local API training inputs and per-endpoint prepared artifacts live
# under data/ and stay out of git by design.
logs/*
!logs/.gitkeep
lora/*
!lora/.gitkeep
config.yaml
__pycache__/
evals/*
handoff
models/
archive/
unsloth*
config.yaml
tmp/
